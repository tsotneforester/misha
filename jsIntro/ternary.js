// Example: Determine if a user is eligible for a discount
const hasMembership = true;
const discountEligible = hasMembership ? 'Eligible' : 'Not eligible';
console.log(discountEligible); //Eligible

// according to this example write theese challenges

//🔰 Write a ternary operator that checks if a person can vote (18+)
const age = 17;
let canVote; //= .....
console.log(canVote);

//🔰 Check if a person is an adult (18 or older), isAdult must be Boolean
const age = 15;
let isAdult; //= .....
console.log(isAdult);

//🔰 Write a ternary that checks both temperature and humidity
const temp = 28;
const humidity = 65;
let dayStatus; //= .....
console.log(dayStatus); // Return "Beach day" if temp > 25 and humidity < 70%, otherwise "Stay home"

//🔰 Check if a number is even or odd
const number = 7;
let numberStatus; //= .....
console.log(numberStatus); // Your code here (should output "Odd")
// Hint: if number devides on 2 without modulo, it is even

// Return "Good morning" if hour is before 12, otherwise "Good afternoon"
const hour = 9;

//🔰  Return "Good morning" if hour is before 12, otherwise "Good afternoon"
const hour = 9;
let sayHello; //= .....
console.log(sayHello); //Your code here (should output "Good morning")
